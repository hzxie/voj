<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns="http://www.springframework.org/schema/beans" 
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xmlns:tx="http://www.springframework.org/schema/tx" 
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:aop="http://www.springframework.org/schema/aop" 
	xmlns:amq="http://activemq.apache.org/schema/core"
	xmlns:jms="http://www.springframework.org/schema/jms" 
	xmlns:util="http://www.springframework.org/schema/util"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans.xsd
	http://www.springframework.org/schema/mvc
	http://www.springframework.org/schema/mvc/spring-mvc.xsd
	http://www.springframework.org/schema/tx
	http://www.springframework.org/schema/tx/spring-tx.xsd
	http://www.springframework.org/schema/context
	http://www.springframework.org/schema/context/spring-context.xsd
	http://www.springframework.org/schema/aop
	http://www.springframework.org/schema/aop/spring-aop.xsd
	http://activemq.apache.org/schema/core
	http://activemq.apache.org/schema/core/activemq-core.xsd
	http://www.springframework.org/schema/jms
	http://www.springframework.org/schema/jms/spring-jms.xsd
	http://www.springframework.org/schema/util 
	http://www.springframework.org/schema/util/spring-util.xsd">
	<!-- Base Components -->
	<context:annotation-config />
	<!-- Exclude MailConfig: it would contribute a real JavaMailSenderImpl that wins the
	     by-type injection into MailSender. The tests inject the mock 'mailSender' bean below. -->
	<context:component-scan base-package="org.verwandlung.voj.web">
		<context:exclude-filter type="assignable"
			expression="org.verwandlung.voj.web.config.MailConfig" />
	</context:component-scan>

	<!-- Property File Location -->
	<!-- Load ONLY voj-test.properties: it is self-contained and carries test-specific values
	     (the "test" database with createDatabaseIfNotExist, the in-VM vm:// broker, empty mail
	     settings). Do NOT also load the production voj.properties here - its jdbc.url points at the
	     "voj" database, and when both files were merged that production value won, so the tests tried
	     to connect to a non-existent "voj" database (passing locally only where one happened to
	     exist, failing on CI). -->
	<context:property-placeholder location="classpath:/voj-test.properties"
		local-override="true" />
	
	<!-- Map simple view name such as "test" into /WEB-INF/test.jsp -->
	<bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<property name="prefix">
			<value>/WEB-INF/views/</value>
		</property>
		<property name="suffix">
			<value>.jsp</value>
		</property>
	</bean>

	<!-- JDBC Data Source. It is assumed you have MySQL running on localhost 
		port 3306 with username root and blank password. Change below if it's not 
		the case -->
	<bean id="dataSource" class="com.alibaba.druid.pool.DruidDataSource"
		init-method="init" destroy-method="close">
		<property name="driverClassName" value="${jdbc.driverClassName}" />
		<property name="url" value="${jdbc.url}" />
		<property name="username" value="${jdbc.username}" />
		<property name="password" value="${jdbc.password}" />
		<property name="initialSize" value="${jdbc.initialSize}" />
		<property name="maxActive" value="${jdbc.maxActive}" />
		<property name="minIdle" value="${jdbc.minIdle}" />
		<property name="maxWait" value="${jdbc.maxWait}" />
		<property name="timeBetweenEvictionRunsMillis" value="${jdbc.timeBetweenEvictionRunsMillis}" />
		<property name="minEvictableIdleTimeMillis" value="${jdbc.minEvictableIdleTimeMillis}" />
		<property name="removeAbandoned" value="${jdbc.removeAbandoned}" />
		<property name="removeAbandonedTimeout" value="${jdbc.removeAbandonedTimeout}" />
		<property name="validationQuery" value="SELECT 1" />
	</bean>
	<bean id="sessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
		<property name="dataSource" ref="dataSource" />
		<property name="mapperLocations" value="classpath:/mappers/*.xml"></property>
	</bean>
	<bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
		<property name="basePackage" value="org.verwandlung.voj.web.mapper" />
		<property name="sqlSessionFactoryBeanName" value="sessionFactory"></property>
	</bean>
	<bean id="transactionManager"
		class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
		<property name="dataSource" ref="dataSource" />
	</bean>
	<tx:annotation-driven transaction-manager="transactionManager"/>

	<!-- Provision the test database on context startup: build the schema, then
	     load the reference data and the test fixtures. This replaces the manual
	     "mysql test < voj.sql" step the CI configs used to run; schema.sql drops
	     and recreates every table, so it is safe to re-run. -->
	<bean class="org.springframework.jdbc.datasource.init.DataSourceInitializer">
		<property name="dataSource" ref="dataSource" />
		<property name="databasePopulator">
			<bean class="org.springframework.jdbc.datasource.init.ResourceDatabasePopulator">
				<property name="sqlScriptEncoding" value="UTF-8" />
				<property name="scripts">
					<list>
						<value>classpath:db/schema.sql</value>
						<value>classpath:db/seed.sql</value>
						<value>classpath:db/test-data.sql</value>
					</list>
				</property>
			</bean>
		</property>
	</bean>

	<!-- Mail Service -->
	<!-- HTML mail bodies are rendered by the shared Thymeleaf engine (templates
	     under classpath:/templates/mail), mirroring the application's MailConfig;
	     FreeMarker has been removed. -->
	<bean id="mailTemplateResolver"
		class="org.thymeleaf.spring6.templateresolver.SpringResourceTemplateResolver">
		<property name="prefix" value="classpath:/templates/" />
		<property name="suffix" value=".html" />
		<property name="templateMode" value="HTML" />
		<property name="characterEncoding" value="UTF-8" />
	</bean>
	<bean id="templateEngine" class="org.thymeleaf.spring6.SpringTemplateEngine">
		<property name="templateResolver" ref="mailTemplateResolver" />
	</bean>
	<!-- The tests never reach a real SMTP server, so the JavaMailSender is a Mockito
	     mock whose send() is a no-op. This keeps service tests (e.g. UserServiceTest)
	     that trigger mail delivery from spawning background threads that fail with
	     AuthenticationFailedException and clutter the test log. -->
	<bean id="mailSender"
		class="org.verwandlung.voj.web.util.TestMailSenderFactory"
		factory-method="createMailSender" />
	<!-- vojMailSender (MailSender) is picked up by component scanning and
	     constructor-injected with the Thymeleaf engine and JavaMailSender. -->

	<!-- Message Service -->
	<bean id="amqConnectionFactory" class="org.apache.activemq.ActiveMQConnectionFactory">
		<constructor-arg index="0" value="${jms.broker.url}" />
	</bean>
	<bean id="connectionFactory" class="org.springframework.jms.connection.CachingConnectionFactory">
		<constructor-arg ref="amqConnectionFactory" />
	</bean> 
	<bean id="defaultDestination" class="org.apache.activemq.command.ActiveMQQueue">
		<constructor-arg index="0" value="vojMessageQueue"/>
	</bean>		
	<bean id="jmsTemplate" class="org.springframework.jms.core.JmsTemplate">
		<property name="connectionFactory" ref="connectionFactory" />
		<property name="defaultDestination" ref="defaultDestination" />
	</bean>
	<bean class="org.springframework.jms.listener.SimpleMessageListenerContainer">
		<property name="connectionFactory" ref="connectionFactory" />
		<property name="destinationName" value="vojMessageQueue" />
		<property name="messageListener" ref="messageReceiver" />
	</bean> 
	<bean id="messageSender" class="org.verwandlung.voj.web.messenger.MessageSender"></bean>
	<bean id="messageReceiver" class="org.verwandlung.voj.web.messenger.MessageReceiver"></bean>
</beans>