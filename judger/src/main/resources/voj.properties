# Database Configuration
jdbc.driverClassName = com.mysql.cj.jdbc.Driver
jdbc.url = jdbc:mysql://localhost:3306/voj?characterEncoding=UTF-8&serverTimezone=UTC
jdbc.username = root
jdbc.password = 
jdbc.initialSize = 5
jdbc.maxActive = 30
jdbc.minIdle = 3
jdbc.maxIdle = 10
jdbc.maxWait = 30000
jdbc.timeBetweenEvictionRunsMillis = 60000
jdbc.minEvictableIdleTimeMillis = 25200000
jdbc.removeAbandoned = true
jdbc.removeAbandonedTimeout = 1800

# Message Service Configuration
jms.broker.url = tcp://localhost:61616

# Authentication for voj 
judger.username = voj@judger
judger.password = zjhzxhz
judger.description = 

# Working Directory
judger.workDir = /tmp
judger.checkpointDir = /tmp/voj-testpoints

# Sandbox: "native" (legacy JNI runner; default; the only option on Windows) or
# "isolate" (Linux only, recommended for production; requires isolate to be installed).
judger.sandbox = native

# isolate sandbox configuration (only used when judger.sandbox = isolate)
judger.isolate.executable = isolate
judger.isolate.boxId = 0
judger.isolate.maxProcesses = 64
judger.isolate.wallTimeBufferSec = 1.0

# System User with Lower Privileges (only used by the native sandbox)
system.username = hzxie
system.password = @AppVeyor