name: Coverage

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:8
        env:
          MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
          MYSQL_DATABASE: test
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping --silent"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=10

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          # Full history and tags are required for the maven-git-versioning
          # extension to resolve the project version via git describe.
          fetch-depth: 0

      - name: Set up JDK
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "25"
          cache: maven

      # No manual schema import: the Spring test context provisions the database
      # by running sql/{schema,seed,test-data}.sql, and the JDBC URL carries
      # createDatabaseIfNotExist=true plus the required sql_mode.

      - name: Install judger native build dependencies
        run: sudo apt-get update && sudo apt-get install -y libseccomp-dev

      - name: Prepare judger test configuration
        run: cp judger/src/test/resources/voj-test-linux.properties judger/src/test/resources/voj-test.properties

      - name: Run tests with coverage
        # judger compiles its JNI library and uses setuid to drop privileges to
        # the "nobody" account (system.username in the test properties), so the
        # reactor runs as root. HOME/JAVA_HOME/PATH are preserved so the cached
        # ~/.m2 repository and the selected JDK are reused.
        run: |
          sudo env \
            "HOME=$HOME" \
            "JAVA_HOME=$JAVA_HOME" \
            "PATH=$PATH" \
            mvn -B test -Dmaven.repo.local="$HOME/.m2/repository"

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: web/target/site/jacoco/jacoco.xml,judger/target/site/jacoco/jacoco.xml
          fail_ci_if_error: false
